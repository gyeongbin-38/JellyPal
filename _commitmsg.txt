feat: rarity reads — ambient trait/legendary fx on site AND product pals

Why: common vs rare vs epic vs legendary pals looked near-identical;
the product only shed trait motes from the main pet, so companion pals
(what you actually see) had no rarity signature.

- product main.js: pals now emit gentler ambient trait motes (spark/
  drip/wisp/glint/royal/bubble) + legendary quirk fx (starburst, embers,
  halo, goldtrail, glow, trail, drool, chips, notes) — gated on awake,
  visible, not-held, reduced-motion off
- site pals.js: port LEG quirk table, WING_F frames + wingImg tinting,
  spider-leg doodad
- site ranch.js: per-pal ambient motes + legendary quirks, movement
  styles (hover float, scurry bursts, blink teleport, bigger hop),
  wings on stella/drago, webby kicks legs while carried, pulsar orbit
  motes + collapse rings, epic+/legendary pull fanfare
- dex: rarity-tinted cell frames (r1/r2/r3) + ◆ signature-move tag
- rebuilt binaries, 57/57 shipcheck, zip==exe hash match
