fix: widen redeem fallback + Stripe pack-by-price mapping

- redeem falls back to offline verification whenever the server never
  answered (offline / bad response / server busy) — real refusals still
  stay final so shared codes die at the boundary
- stripe webhook resolves the pack from PRICE_<cents> vars since payment
  links don't propagate their metadata to the checkout session

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
