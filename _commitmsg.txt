feat: web demo parity — props, dress-up, breeding + night ranch look

- port all 8 prop sprites (SPR table) + accessories (drawAccRaw:
  cap/bow/crown/tophat/specs/party/flower/phones) + makeHybrid engine
- toybox toolbar: place cushion (pals walk over, plop, nap + zzz),
  bounce mat (trampoline launches), plant (sniff visit), cookie jar;
  dress buttons put real accessories on the pal under the cursor
- breed button: 20 jelly cost (earned by typing demo), pick two pals,
  they meet mid-ranch, heart burst, egg wobble, hybrid hatches with
  mixed palette/name — pushed into live SPECIES so dex + pull see it
- night ranch sky: crescent moon + twinkling stars
- section heads get real sprite icons (jelly drop, frog, jar, mirror,
  cushion) instead of text decoration
