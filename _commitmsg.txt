feat: drag props onto the HOME slot to put them away

The SEND HOME strip now appears while dragging furniture too — releasing
a prop over it returns it to the toybox (label reads PUT AWAY). Same
gentle-drop zone as slimes; a normal drop still just moves the prop.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
