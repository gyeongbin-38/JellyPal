fix: per-shape edge margins — wide pals/props no longer clip off-screen

Edge clamps assumed a fixed ~26-40px margin, but flat/puddle pal bodies
run to ~44px half-width at high level — they rendered half off the left
screen edge (visible as a sliced green blob on the taskbar). Pal walk
bounds, flight clamp, summon targets, the pet's lo/hi, and prop drop /
re-anchor clamps now all use the actual body half-width. Bonus fix: the
old winW-relative pet clamp stranded pets on negative-x secondary
monitors.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
