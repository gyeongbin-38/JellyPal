harden save/gacha edges from bug audit

- hybSeq now clamps past every loaded hybrid id — a pre-hybSeq or
  hand-edited save could mint a duplicate "hybN" on the next breed
- owned list is deduped on load for the same reason
- rollSpecies falls back to wider pools instead of returning undefined
  into doPull if a rarity tier ever ends up empty
- stale comment: breeding no longer mentions the removed 25% adult roll

Audit results (verified clean): all sfx.* -> SFX_DEFS, all drawSpr ids
-> SPR, all TREATS ids -> SPR, every random-index site length-guarded,
drag states covered by pointercancel + a 20s watchdog, _sim.cjs
600 frames clean.

Binaries rebuilt (0.2.1) with the audit guards + site hashes updated.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
