feat: user identity + server-bound gem grants (Cloudflare worker backend)

Each install generates a random JP<24> uid shown under MY ID in settings;
the backend stores only sha256(uid). A separate server keypair signs
purchase grants ("JP2G:pack:nonce:tag") the app verifies and credits —
codes get bound to one uid server-side so they can't be shared. Seller
key stays offline; server key lives only in the worker's secrets.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
