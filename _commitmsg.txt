fix: runtime repair — sprite() API misuse broke the live ranch + dex

the one-pal rewrite called sprite(p.spIdx) but the signature is
sprite(faceName, spIdx, sil), referenced a drawPal() that never existed,
and used rng()/jellyImg() that weren't in pals.js — the live hero canvas,
dex grid and all icons rendered nothing since 88c1cde.

- FACE_N index->name map + sprOf()/idleOf() helpers; all sprite() calls
  corrected, canvases measured via .width/.height
- added SPR.gem (product's jelly icon) + jellyImg() + rng() to pals.js
- LEG object flags wired properly (wings/halo/orbit/glow/chips/floaty)
- DESIGN.md: full handoff brief for the commercial workspace —
  live URLs, deploy steps, pals.js API reference, design tokens,
  regression rules, and the remaining design backlog
